public interface IBuffable
{

    // TODO: buradaki modifierlar modifier class'ına(veya interface'ine) dönüştürülebilir
    void AddModifier(float modifier);
    void RemoveModifier(float modifier);
}
